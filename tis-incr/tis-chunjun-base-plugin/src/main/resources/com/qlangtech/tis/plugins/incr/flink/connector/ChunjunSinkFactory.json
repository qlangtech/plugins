{
  "writeMode": {
    "dftVal": "insert",
    "enum": "com.qlangtech.tis.plugin.datax.IWriteModeSupport.supportModes()"
  },
  "semantic": {
    "dftVal": "at-least-once",
    "enum": "com.qlangtech.tis.plugins.incr.flink.connector.ChunjunSinkFactory.getSupportSemantic()"
  },
  "batchSize": {
    "dftVal": 5000
  },
  "flushIntervalMills": {
    "dftVal": 10000,
    "help": "\"the flush interval mills, over this time, asynchronous threads will flush data. The default value is 10s."
  },
  "parallelism": {
    "dftVal": 1,
    "help": "sink 并行度"
  },
  "scriptType": {
    "label": "脚本类型",
    "dftVal": "StreamAPI"
  }
}
