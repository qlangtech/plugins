{
  "name": "mysqlreader",
  "parameter": {
  "connection": [
    {
      "jdbcUrl": ["${reader.jdbcUrl}"],
      "querySql": ["SELECT ${reader.cols} FROM ${reader.sourceEntityName} #if($reader.showWhere) WHERE ${reader.sqlCriteria} #end"]
    }
  ],
  "password": "${reader.password}",
  "username": "${reader.username}"}
}